FROM python:3.11-slim

WORKDIR /app

COPY requirements-api.txt .
RUN pip install --no-cache-dir -r requirements-api.txt

COPY flask_api.py .

ENV PORT=8000
EXPOSE 8000

CMD gunicorn -w 1 --threads 4 -k gthread --timeout 120 --max-requests 200 --max-requests-jitter 50 -b 0.0.0.0:$PORT flask_api:app