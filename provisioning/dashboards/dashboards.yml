apiVersion: 1

datasources:
  - uid: cezrqufqpp6v4e
    name: Thesis API
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: ${API_URL}
    isDefault: true
    jsonData:
      authentication: none
      allowedHosts:
        - ${API_URL}